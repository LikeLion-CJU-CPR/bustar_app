class Achievement {
  final String name;
  final String description;
  final String icon;
  final bool isCompleted;

  Achievement({
    required this.name,
    required this.description,
    required this.icon,
    required this.isCompleted,
  });
}
